{
	"name": "flarelens",
	"private": true,
	"license": "MIT",
	"scripts": {
		"dev": "pnpm -r --parallel dev",
		"preview": "pnpm -r --parallel preview",
		"build": "pnpm -r build",
		"check": "pnpm -r check",
		"lint": "biome check .",
		"format": "biome format --write .",
		"test": "pnpm -r test"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.6",
		"typescript": "^5.9.3",
		"wrangler": "^4.71.0"
	}
}
